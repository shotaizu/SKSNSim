name: runner-for-sksnsim
on:
  push:
    branches:
    - main
    - dev/github-runner
    
jobs:
  generate_documents:
    name: generate PDF documents
    runs-on: ubuntu-22.04
    steps:
    - run: make doc
    
